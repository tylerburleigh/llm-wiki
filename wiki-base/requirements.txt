# Optional Python dependency for PDF ingest support.
#
# Install with: python3 -m pip install -r requirements.txt
# Markdown-only ingest and scripts/wiki-lint.py work without extras.

# Used by /wiki-ingest to convert PDF sources to markdown.
# Invoked inline as: python3 -c "import pymupdf4llm; ..."
pymupdf4llm
